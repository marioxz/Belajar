# install dovecot
sudo apt-get install dovecot-imapd dovecot-pop3d

#During the installation process, you will be asked if you want to create a self-signed SSL certificate, 
choose Yes.

sudo service dovecot restart
